//src/components/ui/Modal/ModalFooter.jsx 

import React from 'react';

const ModalFooter = ({ children }) => {
    return (
        <div className="modal-footer">
            {children}
        </div>
    );
};

export default ModalFooter;
